# Самые полезные CLI-команды для Filament (шпаргалка в таблицах)

## 1. Обновление и применение изменений

| Команда                                   | Что делает                                                                                   | Когда использовать                                                                 |
|-------------------------------------------|----------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------|
| `php artisan filament:upgrade`            | 🚀 Главная! Публикует ассеты, чистит кэш, запускает миграции (если есть)                     | После `composer update` или обновления Filament                                    |
| `php artisan filament:assets`             | Пересобирает и публикует CSS/JS в `public`                                                    | Если пропали стили, иконки или что-то визуально сломалось                          |
| `php artisan optimize:clear`              | Полная очистка всего кэша Laravel (конфиги, роуты, views)                                     | Когда изменил код (например, `$navigationLabel`), а в админке всё по-старому       |
| `php artisan filament:cache-components`   | Кэширует компоненты Filament                                                                 | На продакшене, для ускорения                                                       |
| `php artisan filament:clear-cached-components` | Удаляет кэш компонентов                                                                 | При локальной разработке, если что-то не обновляется                               |

## 2. Создание сущностей

| Команда                                           | Что делает                                                                                   | Опции / комментарии                                                                |
|---------------------------------------------------|----------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------|
| `php artisan make:filament-resource Customer`     | Создаёт полный CRUD-ресурс для модели Customer                                               | `--generate` — автоопределение полей по БД<br>`--simple` — модальное окно вместо страниц |
| `php artisan make:filament-page`                  | Создаёт пустую кастомную страницу в админке                                                  | -                                                                                  |
| `php artisan make:filament-widget`                | Создаёт виджет (статистика, график и т.д.)                                                    | Можно ставить на дашборд или внутрь ресурса                                        |
| `php artisan make:filament-user`                  | Быстро создаёт пользователя через консоль                                                    | Спросит имя, email, пароль                                                         |
| `php artisan make:filament-theme`                 | Создаёт свою тему для кастомизации стилей и цветов                                           | Нужно npm                                                                          |

## 3. Работа с правами (Filament Shield)

| Команда                                   | Что делает                                                                                   | Когда использовать                                                                 |
|-------------------------------------------|----------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------|
| `php artisan shield:generate --all`       | 🔄 Сканирует проект и добавляет новые permissions + policies в БД                            | **Обязательно** после создания каждого нового ресурса                              |
| `php artisan shield:super-admin`          | Создаёт пользователя с ролью Super Admin (полный доступ без проверок)                        | Когда нужен "бог" в системе                                                        |
| `php artisan shield:install`              | Первичная установка Shield (миграции, конфиги)                                               | Один раз при подключении пакета                                                    |

## 4. Отладка и полезное

| Команда                         | Что делает                                                                                   | Зачем                                                                                      |
|---------------------------------|----------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------|
| `php artisan route:list`        | Показывает все маршруты, включая те, что добавил Filament                                    | Когда нужно понять, какой URL за что отвечает                                              |
| `php artisan about`             | Выводит версии Laravel, PHP, пакетов и статус кэша                                            | Быстро проверить, не закэшировался ли старый конфиг                                        |

## 🔥 Быстрые сценарии

### Сценарий 1: Новая таблица → нужна админка
| Шаг | Команда                                                            | Комментарий                          |
|-----|--------------------------------------------------------------------|--------------------------------------|
| 1   | `php artisan make:model Product -m`                                | Модель + миграция                    |
| 2   | `php artisan migrate`                                              | Применить миграцию                   |
| 3   | `php artisan make:filament-resource Product --generate`            | Создать ресурс с автозаполнением      |
| 4   | `php artisan shield:generate --all`                                | Добавить права                       |

### Сценарий 2: Переименовал пункт меню, а он не меняется
| Шаг | Команда                       |
|-----|-------------------------------|
| 1   | `php artisan optimize:clear`  |

### Сценарий 3: Деплой на сервер
| Шаг | Команда                               |
|-----|---------------------------------------|
| 1   | `composer install --no-dev`           |
| 2   | `php artisan migrate`                 |
| 3   | `php artisan filament:upgrade`        | Сделает всё остальное (ассеты, кэш и т.д.) |